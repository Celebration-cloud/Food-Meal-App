const initialState = {
    meals: [],
    category: [],
    favorites: [],
}
export const MealsReducer = (state = in, { type, payload }) => {
    switch (type) {
        // case initialState: return {}
        default: return state
    }
}