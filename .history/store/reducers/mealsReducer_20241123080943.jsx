const initialState = {
    meals: [],
    category: [],
    favorites: [],
}
export const MealsReducer = (state = initialState, { pa{action}, payload }) => {
    switch (type) {
        // case initialState: return {}
        default: return state
    }
}