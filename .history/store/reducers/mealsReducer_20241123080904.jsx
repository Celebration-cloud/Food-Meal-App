const initialState = {
    meals: [],
    category: [],
    favorites: [],
}
export const MealsReducer = (state = initialState, { {act}, payload }) => {
    switch (type) {
        // case initialState: return {}
        default: return state
    }
}